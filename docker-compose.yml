services:
  proxy-alphabet:
    build: .
    container_name: proxy-alphabet-totalrenting
    restart: unless-stopped
    ports:
      - "${PORT:-3002}:${PORT:-3002}"
    environment:
      - PORT=${PORT:-3002}
      - URL_API=${URL_API}
    env_file:
      - .env
