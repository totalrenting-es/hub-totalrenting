import * as alphabet from './alphabet';
import * as settings from './settings';
import * as vixiees from './vixiees';

const services = {
  alphabet,
  settings,
  vixiees
};

export default services;
